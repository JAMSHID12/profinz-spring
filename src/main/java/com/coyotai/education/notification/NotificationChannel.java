package com.coyotai.education.notification;

/** Delivery channels. WhatsApp reaches the parent; in-app reaches the student's account. */
public enum NotificationChannel {
    WHATSAPP,
    IN_APP,
    SMS,
    EMAIL
}
